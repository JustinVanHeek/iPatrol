<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.ipatrol">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewPatrol" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".activityreports.GraffitiReportActivity" />
        <activity android:name=".activityreports.RecklessDrivingReportActivity" />
        <activity android:name=".activityreports.TFAGlassReportActivity" />
        <activity android:name=".activityreports.AbandonedCarReportActivity" />
        <activity android:name=".activityreports.AlcoholReportActivity" />
        <activity android:name=".activityreports.CrimePreventionReportActivity" />
        <activity android:name=".activityreports.IllegalDumpingReportActivity" />
        <activity android:name=".activityreports.MischiefReportActivity" />
        <activity android:name=".activityreports.OtherReportActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="dev.ipatrol.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>